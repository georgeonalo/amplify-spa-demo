{
  "name": "amplify-spa-demo",
  "version": "1.0.0",
  "description": "Demo Single Page Application for AWS Amplify deployment",
  "main": "index.html",
  "scripts": {
    "start": "serve -s dist -l 3000",
    "build": "npm run copy",
    "copy": "mkdir -p dist && cp index.html dist/ && cp -r assets dist/ 2>/dev/null || true",
    "dev": "serve -s . -l 3000"
  },
  "keywords": [
    "spa",
    "amplify",
    "aws",
    "terraform",
    "portfolio"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "dependencies": {}
}
